package machine;

/**
 * enum with state types. These types are needed to switch the current functions of the coffee machine.
 */
public enum MachineState {
    OFF, SHOWMAINMENU, MAKINGCOFFE, NEEDRESOURCES, GIVEMONEY, SHOWRESOURCES
}
